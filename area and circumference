#include <stdio.h>

int main() {
const float pi=3.14;
float a,c,r;
printf("enter your radius=");
scanf("%f" , &r);
a=pi*r*r;
c=2*pi*r*r;
printf("area is %0.3f" , r);
printf("\ncircumference is %0.2f" ,c);
printf("\nthank you");
return 0;
}
